<a href="<%= human_resources_path %>">Back</a>

<h1>Edit Human resource</h1>
<%= render "error_human_resource" %>

<%= form_for(@human_resource, method: "patch") do |f| %>
  <%= render(:partial=> 'form', :locals => {:f => f}) %>
  <br>
  <%= f.submit "Save changes" %>
<% end %>
