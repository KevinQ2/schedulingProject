<a href="<%= human_resources_path %>">Back</a>

<h1>Create new human resource</h1>
<%= render "error_human_resource" %>

<%= form_for(@human_resource, method: 'post') do |f| %>
  <%= render(:partial=> 'form', :locals => {:f => f}) %>
  <br>
  <%= f.submit "Create Human resource" %>
<% end %>
