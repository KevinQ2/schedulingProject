<a href="<%= tasks_path %>">Back</a>

<h1>Create new task</h1>
<%= render "error_task" %>

<div class="form-2">
  <%= form_for(@task, method: 'post') do |f| %>
    <%= render(:partial=> 'form', :locals => {:f => f}) %>
    <br>
    <%= f.submit "Create Task" %>
  <% end %>
</div>
