<a href="<%= organization_users_path %>">Back</a>

<h1>Edit members of organization</h1>
<%= render "error_organization_users" %>

<%= form_for(@organization_user, method: "patch") do |f| %>
  <%= f.label :name, "Username:" %>
  <%= f.text_field :name %>
  <br>
  <%= f.submit "Save changes" %>
<% end %>
