<!-- Check for errors -->
<% if @team.errors.any? %>
  <p>The form contains <%= pluralize(@team.errors.count, "error") %></p>

  <ul>
      <% @team.errors.full_messages.each do |message| %>
          <li><%= message %></li>
      <% end %>
  </ul>

<% end %>
