<a href="<%= teams_path %>">Back</a>

<h1>Create new team</h1>
<%= render "error_team" %>

<div class="form-2">
  <%= form_for(@team, method: 'post') do |f| %>
    <%= render(:partial=> 'form', :locals => {:f => f}) %>
    <br>
    <%= f.submit "Create Team" %>
  <% end %>
</div>
